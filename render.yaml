services:
  - type: web
    name: pdf-rag-app
    runtime: python
    pythonVersion: 3.11.9     # ✅ FIX THIS
    buildCommand: "pip install -r requirements.txt"
    startCommand: "streamlit run app.py --server.port $PORT --server.address 0.0.0.0"
    envVars:
      - key: OPENAI_API_KEY
        sync: false
